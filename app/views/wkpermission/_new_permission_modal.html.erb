<h3 class="title"><%= "new Permission" %></h3>

<%#= form_for(@group, :url => group_permission_path(@group), :remote => true, :method => :post) do |f| %>
<%= form_tag({:controller => "wkpermission", :action => 'add_permission'}, :method => :post, :id => 'query_form') do %>
  <%= render :partial => 'new_permission_form' %>
  <p class="buttons">
    <%= submit_tag l(:button_add) %>
    <%= submit_tag l(:button_cancel), :name => nil, :onclick => "hideModal(this);", :type => 'button' %>
  </p>
<% end %>
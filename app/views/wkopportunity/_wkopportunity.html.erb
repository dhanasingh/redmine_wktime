<label><%= l(:label_opportunity_plural) %></label>
<div>
	<div class="wk-contextual">
			<%= link_to sprite_icon('add', l(:label_new_item, l(:label_opportunity))), url_for(:controller => 'wkopportunity', :action => 'edit', :parentType => currentObj.class.name, :parentId => currentObj.id, id: nil), :class => 'icon icon-add' %>
	</div>
	<%= render partial: "wkopportunity/opportunity_list", locals: { opportunities: currentObj.opportunities} %>
</div>
<label><%= l(:label_contact_plural) %></label>
<div>
	<div class="wk-contextual">
			<%= link_to sprite_icon('add', l(:label_new_item, l(:label_contact))), url_for(:controller => controller_name, :action => 'edit', :parentType => currentObj.class.name, :parentId => currentObj.id), :class => 'icon icon-add' %>
	</div>
	<%= render partial: "wkcontact/contact_list", locals: { contacts: currentObj.contacts} %>
</div>
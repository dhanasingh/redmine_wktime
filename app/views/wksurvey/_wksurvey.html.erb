<label><%= l(:label_survey) %></label>
<% 
    if !params[:account_id].blank?
        id = 'account_id'
    elsif !params[:contact_id].blank?
        id = 'contact_id'
    end
%>
<div>
    <div class="wk-contextual">
		<%= link_to l(:label_new_survey), url_for(id => params[id], :controller => "wksurvey", :action => "edit"), :class => 'icon icon-time-add' %>
    </div>
	<%= render :partial => 'wksurvey/survey_list', :local => {:params => params}%>
</div>